services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pitchedge
      POSTGRES_PASSWORD: pitchedge
      POSTGRES_DB: pitchedge
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
